import type { Metadata, Viewport } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Can I Kite? - Real-time Kitesurf Conditions NL',
  description: 'Check real-time kitesurfing conditions at Dutch spots and get personalized kite size recommendations',
  keywords: 'kitesurfing, kitesurf, wind, conditions, netherlands, kite size calculator',
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}